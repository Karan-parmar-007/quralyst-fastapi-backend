version: 0.2

env:
  variables:
    ECR_REPOSITORY_NAME: "quralyst-backend-dev"

phases:
  pre_build:
    commands:
      - echo "Pre-Build - Logging into Amazon ECR"
      - ECR_REGISTRY="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com"
      - IMAGE_URI="${ECR_REGISTRY}/${ECR_REPOSITORY_NAME}"
      - COMMIT_SHA=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c1-8)
      - echo "ECR Registry  $ECR_REGISTRY"
      - echo "Image URI     $IMAGE_URI"
      - echo "Commit SHA    $COMMIT_SHA"
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $ECR_REGISTRY
      - echo "ECR Login successful"

  build:
    commands:
      - echo "Build - Building Docker image"
      - echo "Build started at $(date)"
      - docker build -t "$IMAGE_URI:$COMMIT_SHA" .
      - echo "Build complete"

  post_build:
    commands:
      - echo "Post-Build - Pushing image to ECR"
      - aws ecr describe-images --repository-name $ECR_REPOSITORY_NAME --image-ids imageTag=$COMMIT_SHA > /dev/null 2>&1 || docker push "$IMAGE_URI:$COMMIT_SHA"
      - echo "Image pushed $IMAGE_URI:$COMMIT_SHA"
      - printf '{"ImageURI":"%s"}' "$IMAGE_URI:$COMMIT_SHA" | tee imageDetail.json
      - echo ""
      - echo "Post-build complete at $(date)"

artifacts:
  files:
    - imageDetail.json
    - appspec.yml
    - scripts/**/*
    - docker-compose.yml
  discard-paths: no

cache:
  paths:
    - "/root/.docker/**/*"
